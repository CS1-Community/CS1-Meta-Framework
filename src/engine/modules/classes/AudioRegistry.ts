import  { Registry } from "../interfaces/Registry";
import  { RegistryString } from "../types/RegistryString";

export class AudioRegistry implements Registry {
  type: RegistryString
  
  constructor(name){
    
  }
  
}